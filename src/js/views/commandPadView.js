class CommandPadView {
  #parentEl = document.querySelector('.number_pad');
  #data;

  render(data) {
    const { expression, result } = data;
  }
} // end CommandView

export default CommandPadView();
